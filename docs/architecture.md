

La API se comunica exclusivamente mediante JSON, siguiendo el estándar application/json. Todas las pruebas de integración (MockMvc) validan que los encabezados de contenido sean correctos para asegurar la interoperabilidad con clientes externos.